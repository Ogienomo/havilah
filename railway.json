{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'echo === ALEMBIC CURRENT === && alembic current 2>&1 && echo === ALEMBIC UPGRADE HEAD === && alembic upgrade head 2>&1 && echo === ALEMBIC DONE === && uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 2'",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
